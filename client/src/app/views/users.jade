div
  acme-navbar
  div.container
    div(ng-if="userIsAdmin")
      div.pull-left
        input(type="text" placeholder="Busqueda" ng-model="query")
        button.btn.btn-default(ng-click="searchQuery()")
          span( class="glyphicon glyphicon-search")
      
        div.alert.no-subscriptions(align="center" ng-hide="users.length > 0") 
          h2 No existen otros usuarios
        div.alert.no-subscriptions(align="center" ng-show="users.length > 0") 
          h2 Usuarios
        div.row.page-header(style="margin: 0px;")
          div.col-xs-6.col-md-6(align="left")
            button#previous-page-button.btn.btn-default(ng-click="previousPage()")
              span.glyphicon.glyphicon-menu-left
          div.col-xs-6.col-md-6(align="right")
            button#next-page-button.btn.btn-default.pull-right(ng-click="nextPage()")
              span.glyphicon.glyphicon-menu-right
          
        div.item-list
          user-item( ng-repeat="user in users" user="user")
    
    div(ng-if="userIsTutor")
      h2 Saludos tutor! 
      div 
        p Aqui puedes agregar estudiantes a seguir. Recibiras información periodicamante de todos los alumnos que agregues en este paso. (A que eventos se han inscrito, si les hace falta entregar documentación o hasta sus resultados!)
      input(type="text" placeholder="estudiante@gmail.com" ng-model="tutor_user_email")
      button.btn.btn-default(ng-click="saveEmail()") 
        p Guardar Estudiante
